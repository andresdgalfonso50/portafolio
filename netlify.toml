[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Redirect para SPA: todas las rutas van a index.html (excepto assets)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
